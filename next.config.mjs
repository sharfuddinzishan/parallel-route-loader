/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // Enable React Strict Mode
  // experimental: {
  //   appDir: true, // Enable app directory support (if using Next.js 13/14 app features)
  // },
  // images: {
  //   domains: ['your-image-domain.com'], // Add any external image domains if applicable
  // },
};

export default nextConfig;
